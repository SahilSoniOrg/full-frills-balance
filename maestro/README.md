# Maestro (dev-client recording)

Flows here target the **Expo dev client + Metro** for local recording (see `scripts/record-onboarding-ios.sh`).

**CI / local** flows live in [`.maestro/`](../.maestro/README.md) and use Detox release builds — no dev launcher.
