<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_sms_triage_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_journal_launcher_background"
    android:orientation="vertical"
    android:paddingLeft="14dp"
    android:paddingTop="12dp"
    android:paddingRight="14dp"
    android:paddingBottom="12dp">

    <!-- Top title: SMS TRIAGE -->
    <TextView
        android:id="@+id/widget_sms_triage_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="SMS TRIAGE"
        android:textAllCaps="true"
        android:textColor="#B8D6EB"
        android:textSize="11sp"
        android:textStyle="bold" />

    <!-- Pending count -->
    <TextView
        android:id="@+id/widget_sms_triage_count"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:maxLines="1"
        android:text="0 Pending"
        android:textColor="#FFFFFF"
        android:textSize="22sp"
        android:textStyle="bold" />

    <!-- Subtitle / preview -->
    <TextView
        android:id="@+id/widget_sms_triage_preview"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:maxLines="1"
        android:text="Tap to review pending transactions"
        android:textColor="#D9EAF6"
        android:textSize="11sp" />

</LinearLayout>
