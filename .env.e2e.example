# Copy to `.env.local` for Detox / iOS simulator E2E builds.
# EXPO_PUBLIC_* is embedded at bundle time (non-secret harness flag).

EXPO_PUBLIC_E2E=1
# Optional fallback when Detox launch args are not read (single-profile local runs)
# EXPO_PUBLIC_E2E_SEED_PROFILE=onboarded
